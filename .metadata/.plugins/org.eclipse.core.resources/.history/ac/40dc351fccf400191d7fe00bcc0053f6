import java.util.*;

public class LongestTrip {
	class Trip{
		String start;
		String end;
		public Trip(String start, String end) {
			this.start=start;
			this.end=end;
		}
	}
	HashMap<Trip, Integer> map=new HashMap<>();
	public String pathCalculator(String input) {
		if(input == null || input.length() == 0) return "Invalid input";
		String[] lines=input.split(" ");
		if()
	}
}
